<h1>All cities</h1>

<table class="table">
  <thead>
    <tr>
      <th scope="col">#</th>
      <th scope="col">Name</th>
      <th scope="col">Lon</th>
      <th scope="col">Lat</th>
    </tr>
  </thead>
  <tbody>
    <% cities.forEach((city,index) => { %>

    <tr>
      <th scope="row"><%= index %></th>
      <td><%= city.name %></td>
      <td><%= city.lon %></td>
      <td><%= city.lat %></td>
    </tr>

    <% }); %>
  </tbody>
</table>
